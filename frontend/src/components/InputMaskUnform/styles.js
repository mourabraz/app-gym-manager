import styled from 'styled-components';

export const Container = styled.div`
  > span {
    margin-left: 10px;
    font-size: 1.2rem;
    font-stretch: italic;
    text-transform: none;
    color: var(--color-error);
  }
`;
